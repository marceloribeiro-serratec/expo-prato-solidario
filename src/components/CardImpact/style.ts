import { StyleSheet } from "react-native";

export const cardImpact = StyleSheet.create({
    container: {
        padding: 20,
        backgroundColor: "rgba(0,109,55,0.4)",
        marginTop: 30,
        flexDirection: "row",
        alignItems: "center",
        gap: 20,
    },
});