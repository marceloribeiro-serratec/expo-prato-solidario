import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    text: {
        fontSize: 14,
        fontWeight: "600",
    },
    buttonAdjust: {
        width: 150,
        marginRight: 8,
        marginBottom: 8,
        borderRadius: 20,
    }
})