import { StyleSheet } from "react-native";

export const buttonIcon = StyleSheet.create({
    buttonIconText: {
        alignItems: "center",
        flexDirection: "row",
        gap: 8,
        justifyContent: "center",
    },
});