import { StyleSheet } from "react-native";

export const title = StyleSheet.create({
    title: {
        fontSize: 18,
        fontWeight: "bold",
        letterSpacing: 0.5,
        flexDirection: "row",
        alignItems: "center",
        justifyContent: "center",
    }
})